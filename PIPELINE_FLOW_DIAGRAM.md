# 🔄 Diagrama do Flow Prefect

## Estrutura Completa do Pipeline

```
┌─────────────────────────────────────────────────────────────────┐
│           inventory_anomaly_detection_flow                     │
│                    (Flow Principal)                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  TASK 1: task_load_data()                                       │
│  ─────────────────────────────────────────────────────────────  │
│  • Carrega dados de estoque e consumo do CSV                    │
│  • Valida estrutura dos dados                                   │
│  • Retorna: (df_completo, df_consumo)                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  TASK 2: task_clean_data()                                      │
│  ─────────────────────────────────────────────────────────────  │
│  • Remove outliers (método IQR)                                  │
│  • Preenche valores faltantes (interpolação)                    │
│  • Remove duplicatas                                            │
│  • Retorna: df_limpo                                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  TASK 3: task_create_features()                                │
│  ─────────────────────────────────────────────────────────────  │
│  • Cria features de lag (1, 7, 30 dias)                         │
│  • Agrupa por produto_id                                        │
│  • Retorna: df_com_features                                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  TASK 4: task_aggregate_data()                                  │
│  ─────────────────────────────────────────────────────────────  │
│  • Agrega dados diários por produto                             │
│  • Calcula médias, somas, min, max, std                         │
│  • Retorna: df_aggregated                                       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  TASK 5: task_train_prophet_models()                           │
│  ─────────────────────────────────────────────────────────────  │
│  • Treina modelo Prophet para cada produto                      │
│  • Gera previsões de 7 dias                                     │
│  • Salva modelos em outputs/models/                             │
│  • Retorna: dict{produto_id: modelo}                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  TASK 6: task_detect_anomalies()                                │
│  ─────────────────────────────────────────────────────────────  │
│  • Treina Isolation Forest                                      │
│  • Detecta anomalias em consumo e estoque                       │
│  • Salva modelo e resultados (CSV/Parquet)                     │
│  • Retorna: (df_com_anomalias, modelo)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   ▼
    ┌──────────────────────┐  ┌──────────────────────┐
    │ TASK 7 (OPCIONAL):    │  │ TASK 8 (OPCIONAL):   │
    │ task_send_alerts()    │  │ task_generate_report()│
    │                       │  │                       │
    │ • Envia alertas       │  │ • Gera PDF            │
    │ • Discord/Teams/Email │  │ • Gráficos e tabelas  │
    │ • Filtra por score    │  │ • Salva em reports/   │
    └──────────────────────┘  └──────────────────────┘
                    │                   │
                    └─────────┬─────────┘
                              ▼
                    ┌──────────────────────┐
                    │   RESULTADOS FINAIS  │
                    │                      │
                    │ • Total registros    │
                    │ • Anomalias          │
                    │ • Modelos treinados  │
                    │ • Alertas enviados   │
                    │ • PDF gerado         │
                    └──────────────────────┘
```

## 📊 Fluxo de Dados

```
CSV Input
    │
    ▼
[Load] → DataFrame Completo + DataFrame Consumo
    │
    ▼
[Clean] → DataFrame Limpo (sem outliers, sem NaN)
    │
    ▼
[Features] → DataFrame com Lags (y_lag_1, y_lag_7, y_lag_30)
    │
    ▼
[Aggregate] → DataFrame Agregado (consumo_mean, estoque_mean, etc.)
    │
    ├─────────────────┐
    │                 │
    ▼                 ▼
[Prophet]      [Isolation Forest]
    │                 │
    │                 ▼
    │         DataFrame com Anomalias
    │                 │
    │                 ├──────────────┐
    │                 │              │
    │                 ▼              ▼
    │            [Alerts]      [PDF Report]
    │                 │              │
    └─────────────────┴──────────────┘
                      │
                      ▼
              Resultados Finais
```

## ⚙️ Configurações de Retry

| Task | Retries | Delay | Motivo |
|------|---------|-------|--------|
| `load_data` | 2 | 60s | Dependência externa (arquivo) |
| `clean_data` | 2 | 60s | Processamento de dados |
| `create_features` | 2 | 60s | Cálculos complexos |
| `aggregate_data` | 2 | 60s | Agregações |
| `train_prophet_models` | 2 | 60s | Treinamento de modelos |
| `detect_anomalies` | 2 | 60s | Treinamento de modelo |
| `send_alerts` | 1 | 30s | Comunicação externa |
| `generate_report` | 1 | 30s | Geração de arquivo |

## 🎯 Dependências entre Tasks

```
task_load_data
    │
    ├─> task_clean_data (usa df_consumo)
    │
    └─> task_create_features (usa df_limpo + df_completo)
            │
            └─> task_train_prophet_models (usa df_com_features)
    
task_load_data
    │
    └─> task_aggregate_data (usa df_completo)
            │
            └─> task_detect_anomalies (usa df_aggregated)
                    │
                    ├─> task_send_alerts (usa df_com_anomalias)
                    │
                    └─> task_generate_report (usa df_com_anomalias)
```

## 📈 Métricas de Execução

**Tempo estimado:** ~5-10 minutos (dependendo do tamanho dos dados)

**Saídas geradas:**
- Modelos Prophet: `outputs/models/prophet_model_*.pkl.gz`
- Modelo Isolation Forest: `outputs/models/isolation_forest_model.pkl.gz`
- Previsões: `outputs/forecast_7d.csv`
- Anomalias: `outputs/anomalies_detected.csv`, `outputs/anomalies_only.csv`
- Relatório PDF: `outputs/reports/anomaly_report_*.pdf`

## 🔍 Logging

Cada task gera logs detalhados:
- Progresso da execução
- Estatísticas dos dados processados
- Erros e warnings
- Tempo de execução

## ✅ Status de Execução

O pipeline foi testado e executado com sucesso:
- ✅ Todas as 8 etapas concluídas
- ✅ 3.655 registros processados
- ✅ 366 anomalias detectadas (10.01%)
- ✅ 5 modelos Prophet treinados
- ✅ PDF gerado com sucesso

